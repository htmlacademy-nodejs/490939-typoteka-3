'use strict';

const DEFAULT_COMMAND = `--help`;

const USER_ARGV_INDEX = 2;

const FILE_NAME = `mocks.json`;

const ExitCode = {
  OK: 0,
  NOK: 1
};

module.exports = {
  DEFAULT_COMMAND,
  USER_ARGV_INDEX,
  FILE_NAME,
  ExitCode
};
